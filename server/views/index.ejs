<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CP 325.1 Express MongoDB Server</title>
</head>

<body>
    <h1>API Data</h1>

    <h2>Users</h2>
    <ul>
        <% users.forEach(user=> { %>
            <li>User ID: <%= user.id %> User Name: <%= user.username %>
            </li>
            <% }); %>
    </ul>

    <h2>Posts</h2>
    <ul>
        <% posts.forEach(post=> { %>
            <li>Post ID: <%= post.id %> Title: <%= post.title %>
            </li>
            <% }); %>
    </ul>

</body>

</html>